"""RAG core sidecar.

Everything under `stub/` is scaffolding that serves fixture data over the real
API contract. The routes in `api/` are the contract itself and stay as the
implementation lands behind them.
"""

__version__ = "0.1.0"
